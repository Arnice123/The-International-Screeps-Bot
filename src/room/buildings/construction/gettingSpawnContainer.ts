/*
This script is here so the spawnContainer is not null
*/

export function spawnContainer(room: Room) {
    const spawnContainerPosition = room.lookForAt(LOOK_STRUCTURES, room.memory.spawnContainerPosition)


   


}
